# 使用說明:DSA 練習指南

這份文件是 `dsa-practice` repo 的參考手冊 —— 說明它的用途、各部分怎麼串在一起、以及你每天該做什麼。

---

## 為什麼要有這個 repo

最終目標是走系統底層的工作(例如 NVIDIA、驅動開發、CUDA)。要走到那裡,你需要兩件事:

1. **邏輯基本功** —— 知道怎麼拆解問題、選對資料結構、判斷效率好壞。這就是 LeetCode / DSA 練習在訓練的東西。
2. **看得見的紀錄** —— 一份 GitHub 歷史,證明你真的持續在做,不是做一次就沒了。

這個 repo 讓這兩件事同時發生。你解的每一題都是一次 commit。一個月後,你的 commit 歷史*就是*證明。

---

## Repo 結構說明

```
dsa-practice/
├── README.md                    ← 計畫 + checklist(做完就打勾)
├── _template.py                 ← 每題新開始前複製這個
├── week1-arrays-strings/        ← LeetCode 題目,依週次/主題分類
├── week2-hashtable-linkedlist/
├── week3-trees-graphs/
├── week4-dp/
└── data-structures/             ← 手刻實作(不用內建套件)
```

**每週的資料夾**放已解決的 LeetCode 題目,一題一個檔案。

**`data-structures/`** 是不同性質的東西 —— 這裡不是 LeetCode 題目,而是你自己動手打造那些題目背後在用的「工具」。例如 `linked_list.py` 沒有用 Python 內建的 list,而是手動建立節點跟指標,因為重點是理解底層在發生什麼,而不是快速解決一個問題。這個資料夾證明的是你理解資料結構「為什麼」這樣運作,而不只是會呼叫它。

---

## `_template.py` 背後的邏輯

每個題目檔案都刻意用同一種格式:

```python
"""
Problem: <題目名稱 + 連結>
Statement (in my own words): ...
My first approach: ...
Final approach: ...
Time complexity: O(?)
Space complexity: O(?)
"""

def solve(...):
    ...
```

「我一開始怎麼想」這段最重要,但也最容易被跳過。寫下你卡在哪、想法怎麼轉變,才是真正在練邏輯 —— 光是程式碼本身教不了你什麼,思考過程才是重點。如果你直接抄一個解法而不寫這段,基本上等於沒學到東西。

---

## 每日流程

1. **從 `README.md` 的 checklist 挑下一個還沒打勾的項目**(照順序來 —— 排序是刻意按難度/主題安排的)。
2. **在 LeetCode 上讀題,先自己想 5-10 分鐘再查任何東西。** 就算想法是錯的也有用,那就是「我一開始怎麼想」要寫的內容。
3. **真的卡住了?** 去看 NeetCode 對這題的講解 —— 但只看到「思路」就先暫停,還沒看程式碼前,自己試著把想法寫成程式碼。
4. **複製 `_template.py`** 到對應的週次資料夾,命名像 `03_valid_anagram.py`(數字代表你解題的順序,不是 LeetCode 原本的編號)。
5. **把 docstring 填好**(題目敘述、一開始的想法、最終想法、複雜度),然後寫解法。
6. **測試它** —— 本機跑一次(`python 03_valid_anagram.py`),用幾個手動測試案例確認能跑。
7. **在 `README.md` 打勾。**
8. **commit 並 push:**
   ```bash
   git add .
   git commit -m "week1: valid anagram"
   git push
   ```
   一題一個 commit,不要等到週末一次全部丟上去。這樣 commit 歷史才會清楚易讀,展現的是持續的進度,而不是一次性傾倒。

---

## 什麼時候該卡關硬撐,什麼時候該來問

- **真的卡住 20 分鐘以上,毫無進展** → 去查思路(不是查程式碼),看懂之後回來自己實作。
- **程式碼能跑,但不確定邏輯或複雜度分析對不對** → 貼過來問我。這是一個很好的求助時機 —— 驗證自己的推理是否正確,本身就是在練這個技能。
- **只是不想動腦、想偷懶** → 還是撐一下。卡關到「想通」那一刻之前的掙扎過程,才是真正在訓練的部分,如果一卡住就馬上求助,反而學不到東西。

---

## 第四週結束後

README 裡在 Week 4 最後有一項「write-up」,這不是交差用的形式主義。花點時間寫一段簡短反思(哪些東西想通了、哪些還是模糊、下次會怎麼做不一樣),好處是:
- 對你自己來說,能真實看到這一個月進步了多少
- 值得從主 README 連結過去,因為它展現的是自我覺察跟反思能力,不只是完成任務 —— 這對之後任何人看這個 repo 都是加分

---

## 「完成」大概長什麼樣子

四週結束後,你應該會有:
- 大約 20 題解決的題目,每一題都有真實的思考紀錄,不只是能跑的程式碼
- `data-structures/` 裡 3-4 個手刻的資料結構
- 一個月內分散、大約 25 個以上的 commit,不是集中在最後一天
- 一段簡短的總結反思

到這個階段,你就同時有了邏輯基本功跟一個拿得出手的 GitHub 成品,可以往下一階段前進(C/C++ → CUDA)。
